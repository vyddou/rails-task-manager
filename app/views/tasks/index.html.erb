<h1>Ma Liste de Tâches</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <%= link_to task.title, task_path(task) %>
    </li>
  <% end %>
</ul>

<%= link_to "Créer une nouvelle tâche", new_task_path, class: "btn btn-success" %>
